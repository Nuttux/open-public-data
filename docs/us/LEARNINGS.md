# Learnings log

One line per pattern transferred from the Paris project (or invented here): what was taken, what had to change, why. This file is the raw material for the "what we can share is everything learned along the way" promise — keep it honest and current.

| Date | Pattern | From | What changed | Why |
|------|---------|------|--------------|-----|
| 2026-07-15 | Repo layout (pipeline raw→stg→core→mart, docs/decisions, website) | open-public-data | — | Proven; keeps prototype from becoming a mess |
| 2026-07-15 | Self-check identities as pipeline tests (MTS `T=ΣD`, Spending Explorer cross-type totals, DataSF budget Revenue=Spending) | invented during US API recon (echoes Ventura budget−actual=variance) | — | Free arithmetic invariants in the sources beat trusting any single extraction recipe |
| 2026-07-15 | Config-driven sync: country YAML + protocol adapter (`configs/countries/us.yaml` + `sync_fiscaldata.py`/`sync_census_popest.py`) | `configs/cities/marseille.yaml` + `sync_city.py` (PA.2) | City YAML listed one portal's datasets; country YAML declares sources per PROTOCOL (`fiscaldata_api`, `census_popest_csv`), each with its own adapter script (ADR-0010 D3) | A new place in an existing protocol = YAML entry, not code |
| 2026-07-15 | Raw = all-STRING explicit schema (`load_table_from_json`), typing once in stg via `us_fd_*` macros | Paris raw loads CSVs with BQ autodetect | Fiscal Data stringifies everything and encodes missing as literal `"null"` — autodetect would mis-type and silently coerce; explicit STRING schema keeps raw byte-faithful to the API | Auditability: raw must show exactly what the source said |
| 2026-07-15 | `dbt_us_*` dataset family via `generate_schema_name` country-prefix mapping (`us_staging` → `dbt_us_staging`) | `dbt_paris_*` naming in dbt_project.yml/profiles.yml | One macro maps `us_`-prefixed custom schemas onto the mirrored family while preserving dev/ci/prod isolation (`dbt_us_dev_local_*` etc.); Paris configs untouched | One warehouse, per-country namespaces, zero France refactor |
| 2026-07-15 | Machine-readable source catalog as a raw table (`raw.us_fiscaldata_catalog`, one row per endpoint×field) feeding provenance columns in marts/exports | Paris hardcodes source labels in export scripts / seed_city_constants | Treasury publishes `services/dtg/metadata/` (labels, units, row definitions, dataset pages) — synced and joined instead of retyped | Zero-hardcode provenance; units/labels update themselves |
| 2026-07-15 | Documented source-anomaly exclusions inside identity tests (monthly deficit ≠ outlays−receipts on 2024-03/2022-09; T9 vs T5 prior-FYTD $47k gap on 2015-05) | new (forced by running the identity tests on full history) | Tests stay exact (±$0.01/±$2M) everywhere else; each exclusion carries the live-verified evidence in a comment | Loosening tolerance globally would blind the test; the source is imperfect, not the pipeline |
| 2026-07-15 | Export data contract (`generated_at`, `source_pipeline`, per-block `source`/`source_url`, `as_of`, completeness flag, units) | France national exports (`export_daily_bread.py`, `sync_etat_lfi`) | Added `months_into_fiscal_year` completeness (US FY Oct–Sep, MTS restates prior months) and explicit `accounting_basis` label (cash vs obligations trap) | Same promise as Paris: every displayed number traceable to a source URL |
| 2026-07-15 | Per-resident scaling from an official population source synced through raw→stg→core | INSEE population pattern (Paris/France) | api.census.gov now requires a key (verified 2026-07-15) → synced the no-auth Vintage 2025 estimates CSV from www2.census.gov instead (with forced IPv4 — the host hangs on IPv6) | Per-capita framing needs a citable denominator, not a constant |
| 2026-07-16 | Fusion primitives ported unchanged into `/us/national` (SectionHead, HeroNumber, KPIGrid, AnimatedNumber, BarRow, BalanceStack) via direct imports, no barrel | `components/fusion/` (France pages) | Nothing in the components — but every value/label passed pre-formatted through `display`/props, because their internal defaults are fr-FR (BarRow's fallback formatter, ChartSource's « : » spacing → local `SourceLine` instead). BalanceStack's aria strings stay locale-driven (shared keys) | ADR-0010 D3: copy-then-diverge only when forced; props-only reuse worked for 6 of 7 primitives |
| 2026-07-16 | US number/date formatting as a local module (`app/us/national/us-format.ts`) | `@/lib/fmt` (fr-FR: "1 234,56", "Md €") | New file, shared formatter untouched: en-US grouping `$1,234.56`, `$5.52T/$827.2B/$13B` magnitude suffixes, typographic minus, UTC-pinned "June 30, 2026" dates | fr-FR formatters feed every France page; US formats are a family concern (schema family ⇒ own formatters), not a parameter |
| 2026-07-16 | Debt time-machine chart forked, not reused (`UsDebtChart` vs `DebtLineChart`) | `components/fusion/DebtLineChart.tsx` | Same ECharts/ink visual language, but: log y-axis (data spans $33,733→$39.4T, 9 orders of magnitude), en-US `$` axis/tooltips, annual series + latest daily kept as two series (export notes forbid splicing), neutral war-era markAreas | The shared primitive hardwires fr-FR "Mds €" and a linear axis — a genuine "primitive can't express it" case |
| 2026-07-16 | EN-only page via mirrored i18n keys (`us.national.*` identical in en.ts and fr.ts) | France FR→EN parity rule | Inverted: FR values are verbatim English. `useT` falls back locale→fr→key, so duplicating into fr.ts is what guarantees English for FR-locale visitors | ADR-0010: US pages EN-only; the flat-dict fallback chain makes "mirror into fr" the cheapest correct mechanism |
| 2026-07-16 | Negative MTS outlay categories rendered outside the ranked bars | invented for US (France budget has no negative-side lines) | Bars show the 17 positive functions; Commerce and Housing Credit + Undistributed Offsetting Receipts sit in a labeled "offsets" block with shares straight from `share_of_side_fytd` | Negatives in a share/length visualization would lie about magnitude; MTS negative outlays are a structural feature of the source, not noise |
| 2026-07-16 | Socrata bulk ingestion: page `/resource/<id>.csv` with `$order=:id` + per-page retry + gzip before BQ load | invented during SF sync | The one-shot `rows.csv` export drops mid-stream on multi-GB pulls (2× observed), no Range/resume | Multi-GB civic datasets need restartable ingestion; NDJSON gzips ~14:1 |
| 2026-07-16 | SODA schema from `/api/views/<id>.json` metadata, never from the first data row | invented during SF sync | SODA JSON omits null keys per row; the same metadata maps bulk-CSV display headers back to API field names | Schema-by-example silently drops sparse columns |
| 2026-07-16 | HTTP retry tuple must be `requests.RequestException` | fix during SF sync | Mid-body drops surface as `ChunkedEncodingError`, which `(ConnectionError, Timeout)` does not catch | Long civic-data pulls fail mid-body more often than at connect |
| 2026-07-16 | Budget-vs-actual only on a measured comparable perimeter (SF: Operating funds, residual −0.4%…−8.8% FY2010–25, COVID outlier −16.8% FY2021) | Paris voté/exécuté pattern, generalized | SF actuals carry a fund category with zero budget counterpart ($1.6–2.4B/FY) + multi-year continuing-project funds → all-funds comparison structurally invalid; exports label the one comparable pair, everything else ships as separate series | Never publish a comparison the arithmetic can't defend (API-RECON D.4) |
| 2026-07-16 | Census `sub-est` places file is Latin-1 (ñ in city names); NST state file is ASCII | fix during SF sync | utf-8 decode fails on the places file; same no-auth path, IPv4-forcing still needed | Encoding is per-file, not per-source |
| 2026-07-16 | `union` is a GoogleSQL reserved word — DataSF employee-comp has a `union` column | fix during SF dbt | Backtick it in stg | Municipal datasets use labor vocabulary that collides with SQL |
| 2026-07-16 | Place registry as a typed manifest (`lib/places.ts`) driving the /us chrome nav | `lib/cities.ts` (JSON registry) + ADR-0010 D2 | Config const, not a pipeline JSON — it carries no sourced numbers; a module is listed only once its route renders (no-dead-links rule); France nav joins post-October | Adding a place or module = a registry entry, not new chrome code |
| 2026-07-16 | `us-format` promoted `app/us/national/` → `lib/us/format.ts` | rule of three (ADR-0010 D3) | Import path only, zero content change — Blocks 1–4 all consume the same formatters | Promote on demonstrated shared need, not speculation |
| 2026-07-16 | French chat/search suppressed on /us via a pathname gate wrapping each client component | ADR-0010 D4 (chat is Paris-welded) | Gate wrapper keeps hook order legal; France routes render byte-identical; the Cmd+K shortcut dies with the component | Suppress at the component, not the layout — the root layout stays shared across countries |
| 2026-07-16 | `DistributionStrip` — NEW shared US primitive (`components/us/`): $-bucket histogram + exact p25–p99 band, year-scrubbed, pure SVG | invented for SF payroll (block 4) | Nothing in fusion renders a histogram or a percentile band evolving over years (Paris never had person-grain data); every string/format comes in via props so the primitive is locale-agnostic from birth; y-scale = share of that year's employees, fixed across years, so scrubbing compares like with like; open top bucket drawn as a distinct span, never a fake bin | The fusion primitives' fr-FR internals are exactly what this component exists to avoid; the next place with person-grain comp data reuses it as-is |
| 2026-07-16 | Salary/OT/benefits split forked local (`CompSplit`), not StackedBarTheme as-is | `components/fusion/StackedBarTheme.tsx` | The shared component hardwires fr-FR € formatting ("Md €"), fx.* locale keys (French for FR-locale visitors → breaks EN-only) and theme-filter links; kept its editorial palette and flat-bar language in ~90 local lines (one slim 100% bar per year, OT share in accent red) | Same test as UsDebtChart: fork when the primitive can't express it — here locale + link semantics, not geometry |
| 2026-07-16 | Small-cell suppression (n≥5) applied IN the marts, with visible pooling and a bounded fold — plus a belt-and-suspenders JSON validator | invented for SF payroll (dial B, person-grain data) | Cells <5 employees pool into a visible per-dept "Other roles" row; pools still <5 fold into the dept total with NO row (defeats subtraction-recovery); dept-years <5 fold into citywide; org rollups computed FROM published rows only; dbt identity tests assert every fold ≤4 people/yr to the cent; `validate_us_sf_payroll_exports.py` re-checks every written file | Privacy rules must live at the warehouse gate (marts could go public like `dbt_paris_*`), not in export scripts; "zero silent suppression" = every pooled dollar visible, every fold méthode-documented |
| 2026-07-16 | Count-only vs dollar-cell distinction in the publication rule | invented for SF payroll | Threshold counts (">$400k: 1 employee in FY2013") publish exactly even under n=5 — they attach no dollar aggregate to any group, and the source itself is public at row level; every $-carrying cell keeps strict n≥5; the choice is stated in each export's privacy block | A blanket n≥5 on counts would kill the 1→1,252 high-earners arc for zero privacy gain; the line is "does a $ aggregate attach to <5 people" |
| 2026-07-16 | Check the source's own taxonomy before planning an AI classification batch | SF payroll enrichment (the anticipated 1,396-title batch) | DataSF ships `job_family` 100% populated (59 codes) — the batch died; what remained was a 60→16 display map + 103 junk codes (the court's own xxC scheme, mayoral staff grades, commissioners), both hand-written seeds in one session; reclass fills junk only, never overrides a source value | The cheapest enrichment is the one the source already did — measure coverage first, classify second |
| 2026-07-16 | Person-grain pre-aggregation as a dbt intermediate (`int_us_sf_comp_employee_year`) | no-bypass layering rule (Paris) | Percentiles, histograms and the OT>salary flags are per-person statements; ~6% of employees have several rows/yr (43,239 rows vs 40,786 people FY2025) — computing on raw rows double-counts people; the $1k salary floor for the OT counter lives here with the FY2019 artifact documented | Grain conversions are model business, not export-script business — the same rule that keeps core row-level |
| 2026-07-16 | Contract-grain mart over a team-member-grain source (prime-dedupe: money from prime rows, flags across ALL rows, sub-only entries excluded) | invented for SF supplier contracts (Block 3) | Paris marchés never had this — DECP rows are already contract-grain | The register's own consumed/remaining columns are unpublishable (−$83.5B); an INFORMATION_SCHEMA dbt test bans them from mart columns structurally, not by review discipline |
| 2026-07-16 | MarchesSearch → SfContractsSearch: three-tier runtime-validated seed chips + synonym-expanded predicate transplanted | app/fr/city/paris/marches/MarchesSearch.tsx | Result cards became a TABLE with paid-progress bars capped at 100% + "exceeds" marker — paid/agreed is the SF story; search-synonyms dict reused unchanged (already bilingual) | No-dead-chips rule ports cleanly: seeds validate against the exact display corpus with the search predicate |
| 2026-07-16 | ContratFiche → SfContractFiche: lead/KPI/term-timeline/sources skeleton transplanted | components/fusion/ContratFiche.tsx | Vulgarization JSON slot replaced by a deterministic title cleaner (LLM batch gated on eval); added a per-FY spend-curve section — SF vouchers give per-contract payment detail Paris never had | Fiche pattern is family-agnostic; the *content* enrichment path is what differs per country |
| 2026-07-16 | Root-level drawer doctrine mirrored to (...)us — interception + full-page fallback | app/@drawer/(...)fr/... (project_drawer_architecture) | Path prefix only; DetailDrawer, breadcrumb stack and theme wrapper worked unchanged | Parallel-route interception is country-agnostic chrome |
| 2026-07-16 | StackedBarTheme NOT transplanted for the type landscape — BarRow with pre-formatted USD display used instead | components/fusion/StackedBarTheme.tsx | Component hardwires € formatting internally (fmtEur, k €/M €/Md €) — same finding as Block 0's 7th primitive | Props-only reuse holds only when formatting enters via props; internal formatters are the fork line |
| 2026-07-16 | Deterministic title cleaner: dept-code-anchored prefix whitelist + SHOUTING recase with no-vowel/acronym/digit-token preservation | à la lib/objet-normalizer (Paris normalizeObjet) | Anchored to each row's own department_code + explicit alias map (PW→DPW, SFMTA→MTA) — a generic caps-token stripper mangles vendor names (KONICA, UCSF); self-tested with 30 live-sampled titles | 59% of SF titles are admin shorthand and the source has no descriptive text at all — the no-LLM fallback must ship with the page |
| 2026-07-16 | execution_status enum on the spend-by-FY mart (in_progress / recently_closed_preliminary ≤9 months post-close / closed) | SF-BUILD-PLAN cross-cutting rule 2 | Implemented locally in mart_us_sf_contract_spend_by_fy with the 9-month window documented as a display heuristic; orchestrator reconciles with Block 1's implementation at merge | The calendar boolean lies 16 days after year-end; the enum names what a reader needs to distrust |
| 2026-07-16 | QuiRecoitExplorer search section transposed to /us/city/sf/who-gets-paid (lazy index, seed chips, min/max filters, result cards) | `app/fr/city/paris/subventions/QuiRecoitExplorer.tsx` | Color dimension theme→bucket, second filter theme→department, fiche links dropped (vendor names unkeyed), French synonym expansion replaced by a local EN normalizer — but the runtime seed-chip validation kept the SAME predicate as the search (no-dead-suggestions) | The skeleton transposes; the language layer (synonyms) is family-specific and must not cross the border |
| 2026-07-16 | Two-layer payee classification with binding view-guard dbt tests (default top-10 clean + latest-FY coverage ≥60%) | Paris mapping seeds (no view guards) | Exact-string seed (232 rows, in-session) + the mart's per-string join carries a bucket into every FY; tests reconstruct the page's default view and fail on unclassified bank-pattern names — the renamed-vendor trap is a test failure, not a silent leak | A display seed feeding a public default view needs its invariants tested like accounting identities |
| 2026-07-16 | Case-variant vendor strings get one seed row per spelling + a case-insensitive bucket-CONSISTENCY test (not uniqueness) | invented during Block 2 (FY2018 legacy-caps vs Title Case duplicates) | First test draft asserted CI-uniqueness and instantly failed on New Flyer ×2 — the portal really publishes both casings as distinct strings | Encode the source's reality (two spellings, one classification), not the tidy assumption |
| 2026-07-16 | "Seed picks, warehouse computes" materiality lines (jail meals, election interpreters, Port lumber…) | `seed_lieux_connus` spirit (curated picks) | Seed carries only (vendor × dept × sub-object × FY) + editorial label; the mart INNER JOINs vouchers for the amount and a dbt test fails if a pick stops matching | Zero-hardcode survives curation: editorial choice in the seed, every dollar from the data |
| 2026-07-16 | `execution_status` enum macro (`us_sf_execution_status`) replacing the calendar boolean in payee marts/exports | SF-BUILD-PLAN cross-cutting rule 2 | closed / recently_closed_preliminary (<4 months post-June-30) / in_progress, computed in one macro shared by all Block-2 marts; the UI maps it to YearPicker preview dots + status banners | A 16-day-old "complete" fiscal year is preliminary — encode close-lag as data, not copy |
| 2026-07-16 | Server-side year slicing via `?year=` for a 1.5MB multi-year export | Paris budget page (`searchParams` + YearPicker) | page.tsx picks one FY's payload out of top_payees.json at render; the RSC payload stays ~40KB and the year picker is plain links | Big per-year exports don't need per-year files if the server slices — same URL semantics, one file to version |
| 2026-07-16 | Personnes-physiques doctrine transposed as a `person` bucket | Paris aggregate + méthode-link doctrine | Individual landlords (study-named, verified) are seed-classified `person`: excluded from every ranking/suggestion/example, reachable in search only by typing the name, labeled "individual payee", explained in the method section | US open data publishes the names; the doctrine still governs what the PAGE features |
| 2026-07-16 | Per-year breakdown export + year-selector swap (`budget_breakdown_{fy}.json`) | Paris budget per-year files | dept×character cells ship as compact arrays (labels resolve through the departments/characters blocks) to keep 18 files at ~130KB; FY2018 file ships with `drill.available=false` + reason instead of being skipped | A corrupted-drill year must stay pickable (citywide totals are fine) — a missing file would read as missing data |
| 2026-07-16 | `execution_status` enum ('closed'/'recently_closed_preliminary'/'in_progress'/'adopted_only') via one macro, replacing the calendar completeness boolean | invented for SF (Block 1 study measured the boolean flagging FY2026 "complete" 16 days after year-end) | Newest-ENDED-FY rule, pure date arithmetic, basis-dependent for the running year (budget ⇒ adopted_only, actuals ⇒ in_progress); self-healing when the next FY ends | Calendar booleans lie during the months-long accounting close; the enum drives the page's 3 year-states directly |
| 2026-07-16 | Root-level drawer architecture transplanted to US (`@drawer/(...)us/city/sf/budget/{dept,character}/[slug]` + full-page fallbacks) | `@drawer/(...)fr/...` (drawer doctrine) | DetailDrawer reused UNCHANGED — its fr-only "view all X" hub button simply doesn't render on /us paths (regex no-match degrades gracefully); fiche side carried as `?side=` param | One global drawer per entity works across countries; only the hub-link map is France-shaped (candidate generalization when a second US entity needs it) |
| 2026-07-16 | `UsTreemap` forked from fusion/BudgetTreemap (same squarify + visual language) | components/fusion/BudgetTreemap.tsx | EUR formatting and the 3-value French-institution `group` enum are hardwired in the shared primitive → USD single-ink ramp, informational cells (no fiche exists at org-group altitude, so no links) | Same "primitive can't express it" precedent as UsDebtChart; 7 org groups need a value ramp, not institution colors |
| 2026-07-16 | YearPicker reused for the SF 3-state picker (votedYears ⇒ adopted-only red, previewYears ⇒ preliminary dashed) | fusion/YearPicker (Paris voted years) | One additive prop (`previewTitle`) parameterizes the only French-hardcoded string; France call sites unchanged via default | Props-only reuse rule held with a 2-line additive change — no fork needed for a shared visual state machine |
| 2026-07-16 | Enrichment seeds guarded by coverage + staleness singular tests (dept names must cover every modern dept; seed's source_label snapshot must still be observed in the source) | Paris enrichment seeds (labels/caches) | New: the tests make seed rot loud — a new dept code or a portal rename FAILS the build instead of silently rendering raw/wrong | Display layers drift; provenance-flagged seeds need an expiry alarm, not just a provenance column |
| 2026-07-16 | Paris s05 voted/exécuté layout (HeroNumber % + KPIGrid + CompareBar) transplanted onto SF's measured Operating perimeter, with the multi-year spine charted as execution RATE | fr/city/paris/budget s05 | Signed residuals (−0.4%…−16.8%) never become bar lengths — bars show actual/budget rate (always positive), residual stays text; GEN/PUC rows wear a "perimeter" chip fed by the seeded annotation | Negatives-never-in-length rule extends to deviation charts: chart the ratio, print the signed number |
